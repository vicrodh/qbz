// crates/qbzd/src/api/mod.rs — HTTP surface (stub). Route handlers and the
// server itself land in T6/T7/T8; this module currently only carries the
// `/api/status` response contract (T2, 02-cli-and-api.md §3.3.3).
pub mod status;
